 <macro name="term-translator-short">
    <choose>
      <if locale="[ZH_LOCALE]" match="any">
        <text value="譯" />
      </if>
      <else>
        <label form="short" prefix=", " text-case="title"/>
      </else>
    </choose>
  </macro>
