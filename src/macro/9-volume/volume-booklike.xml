  <macro name="volume-booklike">
    <choose>
      <if locale="zh jp 中文" match="any">
        <group delimiter="、">
          <text macro="volume-booklike-group-children" />
        </group>
      </if>
      <else>
        <group delimiter=", ">
          <text macro="volume-booklike-group-children" />
        </group>
      </else>
    </choose>
  </macro>